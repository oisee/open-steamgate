# Keep the leading zero: INSTANCE=00, not INSTANCE=0.
INSTANCE=00

# Optional: Docker host address in the generated HTTPS certificate.
# TLS_SAN=DNS:osd.example.test

# HANA only: read SAP's license before setting YES.
HANA_PASSWORD=
ACCEPT_SAP_LICENSE=
